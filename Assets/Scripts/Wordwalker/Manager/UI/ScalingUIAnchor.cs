using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// A class whose only trait is its existence
// When attached to the child of a ScalingUIComponent object (typically a 3D object in the UI layer), it will be scaled
// (thus scaling all its children by proxy) and none of its children will be further modified at all.
public class ScalingUIAnchor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
